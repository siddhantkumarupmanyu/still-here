


export function randomNumberGenerator(upperBound: number) {
    return Math.floor(Math.random() * upperBound);
}

// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/random
